#setFileName[$!{camelCaseTableName}Dao.java] #!定义java文件名称!#
#setFilePath[D:\history-git\git-20200729\lf-open-java-generate-code\src\main\java\com\github\test\dao]

package com.github.test.dao;

import com.gtihub.test.entity.$!{camelCaseTableName};
import org.apache.ibatis.annotations.Param;
import java.util.List;

/**
 * @author : !##{author}##
 * @URL : !##{url}##
 * @CreateTime : !##{nowDate}##
 * @Description : 生成模板映射类- 存储模板对应关系
 * @TableName ：!{tableName} !{tableNote}
 */
public interface !{camelCaseTableName}Dao {

    /**
     * 通过ID查询单条数据
     *
     * @param $!pk.name 主键
     * @return 实例对象
     */
    !{camelCaseTableName} queryById(!{primaryKeyInfo.classTypeShort} !{primaryKeyInfo.fieldName});

    /**
     * 查询指定行数据
     *
     * @param offset 查询起始位置
     * @param limit 查询条数
     * @return 对象列表
     */
    List<!{camelCaseTableName}> queryAllByLimit(@Param("offset") int offset, @Param("limit") int limit);


    /**
     * 通过实体作为筛选条件查询
     *
     * @param !{camelCaseTableName|upper} 实例对象
     * @return 对象列表
     */
    List<!{camelCaseTableName}> queryAll(!{camelCaseTableName} $!{camelCaseTableName|upper});

    /**
     * 新增数据
     *
     * @param $!tool.firstLowerCase($!{tableInfo.name}) 实例对象
     * @return 影响行数
     */
    int insert($!{tableInfo.name} $!tool.firstLowerCase($!{tableInfo.name}));

    /**
     * 修改数据
     *
     * @param $!tool.firstLowerCase($!{tableInfo.name}) 实例对象
     * @return 影响行数
     */
    int update($!{tableInfo.name} $!tool.firstLowerCase($!{tableInfo.name}));

    /**
     * 通过主键删除数据
     *
     * @param $!pk.name 主键
     * @return 影响行数
     */
    int deleteById($!pk.shortType $!pk.name);

}
